FROM gemini-cli-sandbox

# Add your custom dependencies or configurations here
# For example:
RUN apt-get update && apt-get install -y build-essential g++ curl autotools-dev libicu-dev libbz2-dev cmake git screen checkinstall zlib1g-dev libssl-dev bzip2

# COPY ./my-config /app/my-config